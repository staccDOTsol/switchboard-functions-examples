// import apiSpec from "../swagger.js";

// import * as OpenApiValidator from "express-openapi-validator";

// const openApiValidator = OpenApiValidator.middleware({
//   apiSpec: apiSpec,
//   validateRequests: false, // (default=true), lets validate the jobs ourself
//   validateResponses: false, // default false, lets ensure downstream users get response we expect
// });

// export { openApiValidator };

export {};
